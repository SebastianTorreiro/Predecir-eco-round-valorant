import { EcoRoundContainer } from './components/EcoRoundContainer'

function App() {
  return (
    <main style={{ minHeight: '100vh', display: 'flex', flexDirection: 'column', justifyContent: 'center' }}>
      <EcoRoundContainer />
    </main>
  )
}

export default App

